image: python:3.6
variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache"

stages:
  - docs
  - build-base
  - build-meltano
  - pre-test
  - test
  - publish
  - distribute

include:
  - local: .gitlab/ci/global.gitlab-ci.yml
  # manual (git-ops)
  - local: .gitlab/ci/gitops.gitlab-ci.yml

MockTest:
  stage: test
  script:
    - echo "Totally awesome and fantastic test"

MockBase:
  stage: build-base
  script:
    - echo "Totally awesome and fantastic build-base"