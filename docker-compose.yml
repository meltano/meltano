version: '3.1'
services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: changeme

  meltano-base:
    build: 
      dockerfile: app/meltano-analyze/base.Dockerfile
      arg:
        POSTGRES_DB: postgres
        POSTGRES_USER: postgres
        POSTGRES_PASSWORD: changeme
        POSTGRES_URL: postgres
        LOG_PATH: meltano-analyze.log
        FLASK_ENV: production
        API_URL: /
    image: meltano:base

  meltano-full:
