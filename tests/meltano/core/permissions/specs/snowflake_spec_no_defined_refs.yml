# Databases
test_db:
    type: database

# Roles
loader:
    type: role
    warehouses:
        - dev_warehouse
        - loading
    privileges:
        databases:
            read:
                - raw
            write:
                - raw
        schemas:
            read:
                - analytics.*
            write:
                - analytics.*
        tables:
            read:
                - test_db.test.*
            write:
                - test_db.test.*
    owns:
        schemas:
            - dev_db.loader_scratch

# Users
test_user:
    type: user
    can_login: yes
    member_of:
        - sysadmin
        - transformer
        - loader
        - test_user_role

# Warehouses
loading:
    type: warehouse
    size: x-small
