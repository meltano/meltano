apiVersion: v1
kind: Service
metadata:
  name: {{ template "meltano.fullname" . }}
  labels:
    app: {{ template "meltano.name" . }}
    chart: {{ template "meltano.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "meltano.name" . }}
    release: {{ .Release.Name }}
